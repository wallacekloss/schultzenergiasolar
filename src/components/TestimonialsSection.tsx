import { Quote, Star } from "lucide-react";
import { testimonials } from "@/data/testimonials";

export function TestimonialsSection() {
  return <section id="depoimentos" className="section-padding bg-muted/40"><div className="container-max mx-auto"><div className="max-w-3xl mb-10"><p className="text-primary uppercase font-bold text-sm mb-3">Depoimentos reais</p><h2 className="font-display text-3xl md:text-5xl font-bold text-secondary mb-4">O que nossos clientes dizem</h2><p className="text-lg text-muted-foreground">Avaliações de clientes atendidos em bairros de Linhares.</p></div><div className="grid md:grid-cols-2 lg:grid-cols-3 gap-5">{testimonials.map(item => <figure key={item.name} className="bg-background border border-border p-6 rounded-lg"><Quote className="h-8 w-8 text-primary/40 mb-4" /><div className="flex gap-1 mb-4" aria-label="5 de 5 estrelas">{Array.from({ length: item.rating }).map((_, index) => <Star key={index} className="h-4 w-4 fill-primary text-primary" />)}</div><blockquote className="text-muted-foreground leading-relaxed line-clamp-[10]">“{item.text}”</blockquote><figcaption className="mt-5 pt-5 border-t border-border flex items-center gap-3"><span className="w-10 h-10 rounded-full bg-secondary text-secondary-foreground flex items-center justify-center text-sm font-bold">{item.initials}</span><span><strong className="block text-secondary">{item.name}</strong><small className="text-muted-foreground">{item.location}</small></span></figcaption></figure>)}</div></div></section>;
}
